# AGENTS.md

本文件规定任何 AI 助手、自动化代理和参与者在本仓库中的工作方式。仓库是公开的长期状态仓库，安全性和状态一致性优先于推进速度。

## 一、必读顺序

每次开始规划、恢复工作或修改学习状态前，必须按顺序读取：

1. `README.md`：确认仓库用途、当前阶段和公开边界。
2. `AI_CONTEXT.md`：读取完整长期背景、能力起点、时间约束和严格边界。
3. `STATUS.md`：首先检查 `last_updated`、`current_day`、`current_stage`，再检查门禁状态和下一步。
4. `HANDOFF.md`：读取最新跨环境交接快照。
5. `ROADMAP.md`：确认当前阶段允许做什么、禁止提前做什么。
6. `RESOURCES.md`：确认资源是否仍处于待选择状态。
7. `logs/` 中最新日期的日志。
8. `REVIEW.md` 与 `reviews/question-bank.md`：正式学习前检查到期复习题。
9. 与当前任务直接相关的 `notes/` 和 `exercises/` 内容。

如果聊天记忆、口头描述或旧交接与 GitHub 最新提交中的状态文件冲突，以 GitHub 最新提交中的状态文件为准，并明确指出冲突，不得静默拼接互相矛盾的状态。

## 二、状态更新规则

- GitHub 最新提交中的状态文件是学习状态的唯一真值。
- 家庭 Codex 与公司云端 ChatGPT 通过 Markdown 文件交接，不依赖聊天记忆保持状态。
- `STATUS.md` 保存当前结构化状态。每次实际推进后更新 `last_updated`、`current_day`、`current_stage` 和相应状态项。
- 每次制定计划前必须检查 `STATUS.md` 的 `last_updated`、`current_day`、`current_stage`。
- `HANDOFF.md` 只保留最新交接快照，不累积历史；历史事件写入 `logs/YYYY-MM-DD.md`。
- `ROADMAP.md` 只维护阶段、门禁和阶段间关系。初始化门禁完成前，不展开完整知识图谱或 30 天日程。
- `RESOURCES.md` 在知识图谱完成前保持 `pending_knowledge_map`，不得提前锁定完整教材和课程组合。
- 状态文件只记录已经发生或已经由用户明确确认的事实，不把建议、推测或待批准操作写成已完成。
- 修改状态时同步检查 README、ROADMAP、HANDOFF 和当日日志是否需要更新，避免相互矛盾。
- 每次开始新的学习内容前检查间隔复习队列；一次只提出一道到期题，等待用户回答后再校正和继续。
- 复习结果按 `REVIEW.md` 更新，但不能直接替代统一掌握层级所要求的解释、实现、正确性或性能证据。

## 三、掌握层级

所有知识点、练习和评估统一使用以下四级标准，不得另造含义相近但口径不同的等级：

1. 看过；
2. 能够解释；
3. 能够独立实现；
4. 能够分析正确性与性能。

只有存在相应证据时才能提高等级。证据可以是独立解释、独立实现、测试结果、基准结果或性能分析记录。

## 四、代码实验规则

- 所有练习只能使用自行编写的独立代码、合成数据或公开数据。
- 练习放在 `exercises/`，概念和推导笔记放在 `notes/`，过程事件写入 `logs/`。
- 每个实验应记录目标、环境、构建和运行方法、输入来源、正确性验证、观察结果、当前掌握层级和后续问题。
- 性能实验必须先验证正确性，再进行基准测试；区分预热、重复次数、计时范围和编译配置。
- 不根据一次测量下结论；保留足够的环境信息，使实验能够在不暴露私密信息的前提下复现。
- CUDA 实验应明确执行模型、线程层次、数据规模、内存路径、同步点和错误检查。
- C++ 实验应特别关注生命周期、所有权、非拥有对象以及迭代器、指针和引用失效规则。
- 不得把公司代码改名、删注释或抽取片段后当作独立练习；无法确认来源安全时不得写入仓库。

## 五、Git 操作规则

- 修改前后都检查 `git status --short`，明确区分用户已有修改和本次修改。
- 任何提交前都执行内容审阅、`git diff --check` 和公开安全检查。
- `.gitignore` 不能替代对实际变更和暂存区的逐文件审查。
- 未获得用户明确确认时，不执行 `git add`、`git commit` 或 `git push`。
- 不修改远程地址，不强制推送，不重写公开历史，不使用破坏性清理命令，除非用户针对准确目标给予明确授权。
- commit 应保持范围清晰；提交信息应说明状态变化或实验目的，不得包含内部名称或敏感上下文。
- push 后必须核对远程分支和最新提交；只有远程核对完成且用户明确确认后，才能把初始化门禁标记为完成。
- 本地未提交状态不能被当作跨环境唯一真值；跨环境唯一真值始终是 GitHub 最新提交中的状态文件。

## 六、公开仓库安全规则

不得要求、读取、保存、生成或提交以下内容：

- 任何公司源码或由公司源码改写、摘录、反推的实现；
- 客户数据、业务数据、内部测试数据或可关联到客户的数据；
- 内部文档、模型、截图、名称、内部路径、主机信息或网络拓扑；
- 账号、密码、令牌、Cookie、私钥、证书、连接串、环境变量值或其他凭据；
- 未公开算法、架构、性能数字、故障细节或实现细节；
- 任何不确定是否允许公开的工作材料。

执行规则：

- 不向用户索取上述材料；用户意外提供时，不复制到文件，并提醒其移除或轮换相关信息。
- 只使用公开、合成或自行编写的内容，公开数据必须记录来源和许可。
- 提交前检查文件名、文本、二进制、大文件、图片、模型、数据集、环境文件和疑似凭据。
- 不提交本地绝对路径、用户名、设备名或能够暴露工作环境的信息。
- `data/`、`models/`、`private/` 等目录即使被忽略，也不得被视为可以存放工作敏感材料的许可。

## 七、严格范围边界

- 当前项目不涉及点云，不得加入点云学习内容。
- 碰撞检测重构尚未正式分配，不得假设已经是确定任务。
- 编译器和图形学暂时后置。
- 交易系统只是未来可能方向，不是当前主线。
- 当前主线围绕线性代数、现代 C++、高性能系统基础、内存与数据布局、计算机组成与操作系统、并发、性能分析、基准测试、CMake、CUDA 和 CAM 性能优化相关基础交叉推进。

## 八、初始化门禁

在以下事项全部完成前，初始化门禁保持关闭：

1. 初始文件创建完成；
2. 公开安全检查完成；
3. 初始 commit 完成；
4. 首次 push 完成；
5. 远程核对完成；
6. 用户明确确认完成。

门禁关闭期间：

- 不建立完整知识图谱；
- 不制定 30 天计划；
- 不开始 Day 1；
- 不确定完整教材和课程组合。
