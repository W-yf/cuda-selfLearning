# HANDOFF.md

## 最新交接快照

- `last_updated: 2026-08-12`
- `previous_state_commit: 792be5478f529b068ecc7d362b3124d549a1c88a`
- `current_day: 2`
- `current_stage: rolling_learning_iteration`
- `current_stage_status: in_progress`
- `completed_stage: day_1_lifetime_scope_and_dangling`
- `next_task: start_day_2_raii_and_ownership_classification`
- 资源状态：`selected_user_confirmed`。
- 学习媒介偏好：`video_first`；视频必须与笔记、习题、独立解释或代码实验形成证据闭环。
- 学习环境分配：家中用于线性代数等深度学习；工作场所用于可拆分的公开 C++ 知识学习，且不得接触任何公司代码、内部资料或可识别上下文。
- 现代 C++ 范围：C++11、C++14、C++17、C++20 必学；C++23 选择性桥接；C++26 仅在工作草案、可靠资料和工具链支持可核对时纳入。

## 当前学习证据

- Day 1 已完成：主题为对象生命周期、作用域与悬空对象。
- [Day 1 笔记](notes/day-01-lifetime.md)与[合成练习](exercises/day-01-lifetime/README.md)已经建立；安全路径由 MSVC 14.52 以 C++17、`/W4`、`/permissive-` 编译成功，并以退出码 0 运行。
- 用户能够解释按值返回与返回局部引用/指针的生命周期差异，以及悬空访问偶尔读到旧值仍属于未定义行为。
- 用户能够判断局部引用、逃逸指针和 Lambda 引用捕获形成的悬空关系。
- 最终迁移检查通过：在新的 `std::string` Lambda 场景中，用户独立判断 `[&text]` 不安全，改为 `[text]`，并说明局部对象结束生命周期后不能继续观察它。
- 生命周期主题当前达到“2. 能够解释”；这不表示已经达到独立实现或正确性与性能分析层级。
- 新识别的后续断点：需要系统学习 Lambda 的闭包类型、`auto` 推导结果、捕获规则和 `std::function`；该内容不在 Day 1 虚增层级。
- Day 2 尚未开始，既定主题为 RAII 与所有权分类，完成证据是为 6 个合成场景选择值、引用、裸指针、`unique_ptr` 或 `shared_ptr` 并说明理由。

## 工作区与 Git

- Stage 1 已进入提交 `792be54`；本交接快照覆盖随后完成的 Stage 2、Stage 3 和 Day 1，并作为本次跨电脑同步范围。
- `logs/2026-08-08.md` 仍有已知的换行状态项；没有有意修改其语义内容，提交前需单独复核。
- 跨环境接管时必须以远程最新提交中的状态文件为准；聊天摘要不能替代仓库状态。

## 当前强制约束

- 不加入点云学习内容，不假设未正式分配的具体重构已经成为任务。
- 编译器实现和图形学暂时后置；交易系统不是当前主线。
- 不得接触或写入公司源码、客户数据、内部资料、模型、截图、名称、内部路径、账号、凭据或未公开实现细节。
- 未经用户明确确认，不执行 `git add`、`git commit` 或 `git push`。

## 下一位协作者应执行

1. 按 `AGENTS.md` 规定顺序读取状态文件和最新日志。
2. 检查 `git status --short`，保留现有未提交修改，并单独识别旧日志换行状态项。
3. 从 Day 2 的 RAII 与所有权分类开始，不重复 Day 1。
4. 逐一给出 6 个公开合成场景；每次只问一个问题，并根据用户解释记录证据。
5. 未经用户明确确认，不暂存、提交或推送。

本文件只保留最新交接快照；详细历史过程见 [logs/2026-08-12.md](logs/2026-08-12.md)。
