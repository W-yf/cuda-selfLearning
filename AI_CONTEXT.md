# AI_CONTEXT.md

## 文档用途

本文件完整保存“CUDA / C++ 高性能系统开发学习主线”的长期背景、能力起点、时间约束、协作规则和严格边界。家庭 Codex 与公司云端 ChatGPT 应通过本仓库中的 Markdown 文件交接；聊天记忆与仓库冲突时，以 GitHub 最新提交中的状态文件为准。

## 长期方向

- 线性代数优先；
- 现代 C++、C++ 高性能系统开发；
- 内存层次、数据布局、计算机组成、操作系统；
- 并发、性能分析、基准测试；
- CMake 和工程能力；
- CUDA；
- CAM 性能优化相关基础；
- 数学、C++ 实验、性能分析和 CUDA 交叉推进。

这些方向共同构成长期主线，不应被拆成互不关联的课程清单。后续知识图谱需要体现数学基础、语言与系统能力、实验、正确性验证、性能测量和 CUDA 实践之间的交叉关系，但初始化门禁完成前不得提前建立完整知识图谱。

## 个人背景与能力起点

- 有实际 C++ 工作和功能开发经验；
- 团队中目前只有我主要使用 C++；
- 使用过 `vector`、`list`、`map`、`queue`，并用 `queue` 实现过 BFS；
- 生命周期、非拥有对象、迭代器/指针/引用失效规则仍需系统补强；
- `std::string_view` 尚未正式学习；
- 对缓存局部性和 AoS/SoA 有初步认识；
- 缓存行、缓存一致性、伪共享和对齐需要从基础学习；
- 并发理论接触多于实际工程经验；
- CMake 接近零基础；
- 线性代数需要重新系统学习；
- CUDA 需要从执行模型、线程层次和内存模型重新建立体系。

后续规划必须从上述真实起点出发：既不能把已有 C++ 功能开发经验视为零，也不能因有工作经验而跳过尚不扎实的生命周期、内存、工程和数学基础。

## 时间约束

- 工作日每天约 1 小时；
- 周末两天合计至少 6 小时；
- 计划必须适应长期持续推进，避免把工作日安排成无法完成的大块任务；
- 数学、C++ 实验、性能分析和 CUDA 应交叉推进，但具体节奏要等初始化门禁完成、知识图谱建立后再确定。

## 资料语言与选择原则

- 英文权威资料作为长期主资料；
- 中文作为前期辅助；
- 初始化阶段不提前确定完整教材和课程组合；
- 完整资源组合必须等知识图谱和学习依赖明确后再选择。

## 严格边界

- 当前项目不涉及点云，不得加入点云学习内容；
- 碰撞检测重构尚未正式分配，不得假设已经是确定任务；
- 编译器和图形学暂时后置；
- 交易系统只是未来可能方向，不是当前主线；
- 不得要求、读取、保存或提交任何公司源码、客户数据、内部文档、模型、截图、名称、内部路径、账号、凭据或未公开实现细节；
- 所有练习只能使用自行编写的独立代码、合成数据或公开数据。

如果某项学习建议需要引用工作场景，只能使用抽象、公开、不可反推出公司或客户信息的描述。不得让用户提供材料来“帮助判断是否敏感”；有疑问时直接排除。

## 掌握层级

所有主题统一使用以下层级：

1. 看过；
2. 能够解释；
3. 能够独立实现；
4. 能够分析正确性与性能。

层级必须由可检查的学习证据支持。阅读完成只能证明“看过”；能够复述不自动等于能够独立实现；实现能够运行也不自动等于能够分析正确性与性能。

## 跨环境规则

- GitHub 最新提交中的状态文件是学习状态的唯一真值；
- 聊天记忆与仓库冲突时，以仓库为准；
- 家庭 Codex 与公司云端 ChatGPT 通过 Markdown 文件交接；
- `HANDOFF.md` 只保留最新交接快照；
- 每次规划前检查 `STATUS.md` 的 `last_updated`、`current_day`、`current_stage`。

本地未提交修改仍需人工确认，不能替代 GitHub 最新提交成为跨环境真值。交接时应明确哪些内容尚未 commit 或 push。

## 当前初始化状态

- 日期：2026-08-08；
- `current_day: 0`；
- `current_stage: repository_initialization`；
- 公开 GitHub 仓库已创建；
- 本地仓库已成功克隆；
- 本次初始化开始时，初始文件尚未创建；当前约定文件已在本地工作区创建，但尚未暂存；
- 尚无初始 commit；
- 尚未首次 push。

## 初始化门禁

在初始文件创建、公开安全检查、初始 commit、首次 push、远程核对及用户明确确认完成之前：

- 不建立完整知识图谱；
- 不制定 30 天计划；
- 不开始 Day 1；
- 不确定完整教材和课程组合。

门禁的目的不是拖延学习，而是先建立可靠、安全、可跨环境同步的状态基础。任何助手都不得把门禁后的事项提前包装成“预览版完整计划”或以其他名称绕过门禁。
