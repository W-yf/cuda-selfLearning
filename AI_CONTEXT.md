# AI_CONTEXT.md

## 文档用途

本文件完整保存“CUDA / C++ 高性能系统开发学习主线”的长期背景、能力起点、时间约束、协作规则和严格边界。家庭 Codex 与公司云端 ChatGPT 应通过本仓库中的 Markdown 文件交接；聊天记忆与仓库冲突时，以 GitHub 最新提交中的状态文件为准。

## 长期方向

- 线性代数优先；
- 现代 C++、C++ 高性能系统开发；至少系统覆盖 C++11、C++14、C++17 和 C++20，C++23 选择性桥接，C++26 在资料可靠且工具链支持可核对时纳入；
- 内存层次、数据布局、计算机组成、操作系统；
- 并发、性能分析、基准测试；
- CMake 和工程能力；
- CUDA；
- CAM 性能优化相关基础；
- 数学、C++ 实验、性能分析和 CUDA 交叉推进。

这些方向共同构成长期主线，不应被拆成互不关联的课程清单。后续知识图谱需要体现数学基础、语言与系统能力、实验、正确性验证、性能测量和 CUDA 实践之间的交叉关系，但初始化门禁完成前不得提前建立完整知识图谱。

## 个人背景与能力起点

- 有实际 C++ 工作和功能开发经验；
- 团队中目前只有我主要使用 C++；
- 使用过 `vector`、`list`、`map`、`queue`，并用 `queue` 实现过 BFS；
- 生命周期、非拥有对象、迭代器/指针/引用失效规则仍需系统补强；
- `std::string_view` 尚未正式学习；
- 对缓存局部性和 AoS/SoA 有初步认识；
- 缓存行、缓存一致性、伪共享和对齐需要从基础学习；
- 并发理论接触多于实际工程经验；
- CMake 接近零基础；
- 线性代数需要重新系统学习；
- CUDA 完全没有学习过，需要从 host/device、执行模型、线程层次和内存模型开始建立体系。

后续规划必须从上述真实起点出发：既不能把已有 C++ 功能开发经验视为零，也不能因有工作经验而跳过尚不扎实的生命周期、内存、工程和数学基础。

## 时间约束

- 工作日每天约 1 小时；
- 周末两天合计至少 6 小时；
- 计划必须适应长期持续推进，避免把工作日安排成无法完成的大块任务；
- 需要安静环境、较长连续时间和系统记笔记的知识安排在家中学习，线性代数属于这一类；
- 工作场所优先安排能够拆成短单元的公开 C++ 知识学习；不得结合、读取或记录任何公司代码、内部资料、业务数据或可识别上下文；
- 数学、C++ 实验、性能分析和 CUDA 应交叉推进，但具体节奏要等初始化门禁完成、知识图谱建立后再确定。

## 资料语言与选择原则

- 英文权威资料作为长期主资料；
- 中文作为前期辅助；
- 用户学习媒介偏好为视频优先；优先用结构化视频建立概念框架，再用笔记、习题、独立解释和代码实验形成可检查证据，不能把“观看完成”当作掌握；
- 初始化阶段不提前确定完整教材和课程组合；
- 完整资源组合必须等知识图谱和学习依赖明确后再选择。

## 现代 C++ 版本范围

- 用户要求现代 C++ 学习至少包含 C++11、C++14、C++17 和 C++20；“覆盖”必须包含核心语言、代表性标准库、工程使用、版本/兼容边界和独立证据，不以观看视频或浏览特性清单代替掌握；
- C++23 作为从 C++20 到更新标准的选择性桥梁，优先学习与错误处理、矩阵/多维数据、范围和工程表达力直接相关的设施；
- C++26 可以学习，但必须先核对 WG21 工作草案、可靠参考资料和当前编译器/标准库支持；草案功能不得写成已经稳定发布或已经掌握；
- 第一轮 30 个学习日优先修复生命周期、所有权、复制移动、失效、CMake 和性能基础，后续通过多个滚动周期完成版本覆盖，不能为追求版本数量压缩成走马观花。

## 严格边界

- 当前项目不涉及点云，不得加入点云学习内容；
- 碰撞检测重构尚未正式分配，不得假设已经是确定任务；
- 编译器和图形学暂时后置；
- 交易系统只是未来可能方向，不是当前主线；
- 不得要求、读取、保存或提交任何公司源码、客户数据、内部文档、模型、截图、名称、内部路径、账号、凭据或未公开实现细节；
- 所有练习只能使用自行编写的独立代码、合成数据或公开数据。

如果某项学习建议需要引用工作场景，只能使用抽象、公开、不可反推出公司或客户信息的描述。不得让用户提供材料来“帮助判断是否敏感”；有疑问时直接排除。

## 掌握层级

所有主题统一使用以下层级：

1. 看过；
2. 能够解释；
3. 能够独立实现；
4. 能够分析正确性与性能。

层级必须由可检查的学习证据支持。阅读完成只能证明“看过”；能够复述不自动等于能够独立实现；实现能够运行也不自动等于能够分析正确性与性能。

## 跨环境规则

- GitHub 最新提交中的状态文件是学习状态的唯一真值；
- 聊天记忆与仓库冲突时，以仓库为准；
- 家庭 Codex 与公司云端 ChatGPT 通过 Markdown 文件交接；
- `HANDOFF.md` 只保留最新交接快照；
- 每次规划前检查 `STATUS.md` 的 `last_updated`、`current_day`、`current_stage`。

本地未提交修改仍需人工确认，不能替代 GitHub 最新提交成为跨环境真值。交接时应明确哪些内容尚未 commit 或 push。

## 仓库初始化历史

- 日期：2026-08-08；
- 公开 GitHub 仓库已创建；
- 本地仓库已成功克隆；
- 本次初始化开始时，初始文件尚未创建；当前约定文件已经创建并进入初始提交；
- 初始 commit 为 `bf6a428`；
- 首次 push 已成功；
- `main` 与 `origin/main` 已核对同步；
- 用户已明确确认完成仓库初始化阶段的状态收尾。
- Stage 0 状态收尾已经进入本地提交 `1b176c6`。

## 初始化门禁结果

以下初始化门禁条件均已完成：

- 初始文件创建；
- 公开安全检查；
- 初始 commit；
- 首次 push；
- 远程核对；
- 用户明确确认。

初始化期间遵守了以下限制：未建立完整知识图谱、未制定 30 天计划、未开始 Day 1、未确定完整教材和课程组合。

初始化阶段没有生成知识图谱、30 天计划、Day 1 内容或完整资源组合。

## 当前阶段状态

- 日期：2026-08-12；
- `current_day: 2`；
- `current_stage: rolling_learning_iteration`；
- `current_stage_status: in_progress`；
- 知识图谱已经建立，包含主题依赖、四类优先级、目标层级和证据门槛；
- 能力基线清单已经根据用户确认的公开抽象事实建立，B1–B7 验证或起点诊断已经完成；
- 用户已通过“进行下一步”确认 Stage 1 结果，Stage 1 状态为 `completed`；
- Stage 1 结果已进入提交 `792be54`，本地 `main` 与本地跟踪引用 `origin/main` 均指向该提交；实时远程二次核对曾因网络不可用而失败；
- 用户已通过“继续下一步”确认 Stage 2 资源组合；随后明确确认 30 个学习日日程、视频索引和现代 C++ 标准范围，Stage 3 `schedule_design` 已完成。
- Day 1 已完成，对象生命周期、作用域与悬空对象达到“2. 能够解释”；当前准备开始 Day 2 的 RAII 与所有权分类。
